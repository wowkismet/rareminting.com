{
  "name": "rare-minting",
  "version": "0.1.0",
  "private": true,
  "description": "Rare Minting — heritage marketplace for fancy-number currency notes, rare coins and collectibles.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
