{
  "name": "@rareminting/api",
  "version": "0.1.0",
  "private": true,
  "description": "Rare Minting core API: accounts, sellers, listings, orders.",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rareminting/serial-engine": "*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.16",
    "@rareminting/db": "*",
    "@types/node": "^24.3.0",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2"
  }
}
