{
  "name": "@rareminting/db",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL schema, migrations and seed data for Rare Minting.",
  "type": "module",
  "scripts": {
    "test": "node --test \"test/**/*.test.ts\"",
    "migrate:check": "node src/apply.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.16",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
